ODOO_URL=http://172.104.214.173:8069
ODOO_DB=Distefano
ODOO_USER=
ODOO_PASSWORD=
ODOO_COMPANY_ID=1
ODOO_FAKE=0
ODOO_TELAS_CATEG_ID=368    # product.category raíz "Telas" — filtra las sugerencias de /api/odoo/productos

SESSION_SECRET=            # >=32 chars, cifra la cookie de sesión Y firma el token del QR
REQUIRE_ODOO_GROUPS=base.group_user   # grupos Odoo permitidos (coma). Para restringir a Importaciones: distefano_importaciones.group_importaciones_user,distefano_importaciones.group_importaciones_manager
EXTRA_ALLOWED_LOGINS=                  # logins extra permitidos (coma), opcional

# ── Impresión de stickers (Camino A: cola en la nube) ──
KV_REST_API_URL=           # automática al conectar KV Storage en Vercel
KV_REST_API_TOKEN=         # automática al conectar KV Storage en Vercel
PRINT_PULL_TOKEN=          # secreto: autoriza al poller en /api/print/jobs (el MISMO va en poll-agent.bat)
